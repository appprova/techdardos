<%= simple_form_for(@game) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :challenger_points %>
    <%= f.input :challenged_points %>
  </div>

  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>
